//var _ms = delta_time / 1000;

//if (++frames >= 100)
//{
//	sum += _ms;
//	++count;
//}

//draw_text(10, 10, string_format(_ms, 0, 2) + " ms (avg " + string_format(sum / (count > 0 ? count : 1), 0, 2) + " ms)");
